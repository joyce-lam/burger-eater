<li>
	{{burger_name}}
		{{#if devoured}}
			<button class="devoured" data-id="{{id}}" data-devour="{{devoured}}" disabled>Devoured</button>
		{{else}}
			<button class="devoured" data-id="{{id}}" data-devour="{{devoured}}">Devour It!</button>
		{{/if}}
</li>
