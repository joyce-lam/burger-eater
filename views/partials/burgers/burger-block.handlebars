<li>
	<h3>{{burger_name}}</h3>
		{{#if devoured}}
			<button class="devoured" data-id="{{id}}" data-devour="{{devoured}}" disabled>Already Eaten...</button>
		{{else}}
			<button class="devoured" data-id="{{id}}" data-devour="{{devoured}}">Eat Me!</button>
		{{/if}}
</li>
